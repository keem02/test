# 숫자 = [1, 2, 3, 4, 5]
# 승수 = [2, 2, 2, 3, 3]

# #문제
# #1. 숫자의 승수를 zip으로 맵핑해서 진행하세요.
# #2. 숫자를 승수만큼 제곱하여 결과값을 표현해주세요.
# #3. 승수한 값이 100이상인 값을 출력하세요.
# #4. 승수한 값을 모두 더하세요.

# def 제곱(x):
#     return x ** 2
# list(map(제곱, [1, 2, 3, 4]))



# # 1. 숫자와 승수를 zip으로 묶기
# # 2. 숫자를 승수만큼 제곱
# 승수한_값 = list(map(lambda x: x[0] ** x[1], zip(숫자, 승수)))

# # 3. 승수한 값 중 100 이상인 값 출력
# 백이상 = list(filter(lambda x: x >= 100, 승수한_값))

# # 4. 승수한 값 모두 더하기
# 총합 = sum(승수한_값)

# # 결과 출력
# print("승수한 값:", 승수한_값)
# print("100 이상인 값:", 백이상)
# print("승수한 값의 총합:", 총합)



숫자 = [1, 2, 3, 4, 5]
승수 = [2, 2, 2, 3, 3]

# 1. 숫자와 승수를 zip으로 묶고, 제곱 계산
result = map(lambda x: x[0] ** x[1], zip(숫자, 승수))

# 2. 결과값 중 100 이상인 값 필터링
백이상 = filter(lambda x: x >= 100, result)

list comprehension 사용하는 방법 - 주승

#1 
li = list(zip(숫자, 승수))

print(list(map(lambda x,y:x ** y,li)))